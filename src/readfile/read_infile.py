from ..common.global_constants import gls, glc


def read_infile(opt, file: gls().default_file):
    obsr = gls().obs
    obsb = gls().obs
    nav = gls().nav
    imu = gls().imu

    if not file.obsr == "":

        pass
    else:
        pass
